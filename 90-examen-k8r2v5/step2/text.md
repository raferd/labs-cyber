## Parte 2. Integridad y procesos

El equipo técnico ha proporcionado tres archivos en:

```text
/opt/examen/expediente/evidencias
```

Uno es el archivo tomado como referencia y los otros dos son copias que deben
compararse con él.

### P3

**¿Cuál de las dos copias, `copia_A.txt` o `copia_B.txt`, tiene exactamente
el mismo contenido que `original.txt` según su hash SHA-256?**

Anote únicamente `A` o `B`.

### P4

En:

```text
/opt/examen/expediente/sistema/proceso.pid
```

se ha guardado el PID de un proceso que continúa ejecutándose durante el
examen.

**¿Qué programa corresponde a ese PID?**

Anote el nombre del programa para responder en Moodle.

Cuando haya obtenido ambas respuestas, pulse **NEXT**.
