## 2. Permisos

Muestre por pantalla un listado largo de los archivos de la carpeta de documentos utilizando la siguiente instrucción:

```text
ls -l /opt/examen10/documentos
```

### P3
Un usuario que no es propietario del archivo `contrato.txt` y tampoco pertenece al grupo `jurídico`,
¿tiene permiso de **lectura** sobre el archivo `contrato.txt`?

Anote la respuesta y pulse **NEXT**.
